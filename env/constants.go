package env

const (
	// EnvironmentKey is the environment variable that controls the runtime environment.
	EnvironmentKey = "ENVIRONMENT"

	// LocalEnvironment is the default environment when ENVIRONMENT is not set.
	LocalEnvironment = "local"
)
